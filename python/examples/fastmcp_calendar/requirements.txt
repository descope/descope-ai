# FastMCP Calendar example dependencies
# Install the SDK in development mode from the parent directory first:
# cd ../.. && pip install -e .

# Core dependencies (included when SDK is installed)
mcp>=1.0.0
fastmcp>=2.0.0
descope>=1.0.0
pydantic>=2.0.0
typing-extensions>=4.0.0

# Additional dependencies for this example
httpx>=0.24.0
